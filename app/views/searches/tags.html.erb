<div class="container pb-5 mb-5 m-bottom">
  <h5 class="d-sm d-md-none mt-5 mb-5 text-center"><span class="marker-underline"><strong>不妊原因･疾患</strong></span>&nbsp;からレポコを探す</h5>
  <h4 class="d-none d-md-block col-md-8 offset-md-2 mt-5 mb-4"><span class="marker-underline"><strong>不妊原因･疾患</strong></span>&nbsp;からレポコを探す</h4>
  <small class="text-muted d-flex d-md-none col-md-8 offset-md-2 mt-4 mb-4">
    <i data-feather="chevrons-left" class="feather-icon-style1 pb-1"></i>&nbsp;&nbsp;<%= link_to "カテゴリ一覧", search_path %>
  </small>
  <small class="text-muted d-none d-md-block col-md-8 offset-md-2 mt-4 mb-4">
    <i data-feather="chevrons-left" class="feather-icon-style1 pb-1"></i>&nbsp;&nbsp;<%= link_to "カテゴリ一覧", search_path %>
  </small>

  <div class="row px-3">
    <div class="col-md-8 offset-md-2 px-0">
      <% @f_funin_factors.each do |f_funin_factor| %>
        <span class="btn btn-primary border border-primary rounded-pill mr-2 mb-3"><%= link_to f_funin_factor.name, "/tags/factor/#{f_funin_factor.id}", class: "text-dark" %></span>
      <% end %>
      <% @special_examinations.each do |special_examination| %>
        <span class="btn btn-primary border border-primary rounded-pill mr-2 mb-3">
          <%= link_to special_examination.str_special_inspection_name(special_examination.name), "/tags/option/#{special_examination.name}", class: "text-dark" %>
        </span>
      <% end %>
      <% @fuiku_inspections.each do |fuiku_inspection| %>
        <span class="btn btn-primary border border-primary rounded-pill mr-2 mb-3"><%= link_to fuiku_inspection.name, "/tags/fuiku/#{fuiku_inspection.id}", class: "text-dark" %></span>
      <% end %>
    </div>
  </div>
</div>