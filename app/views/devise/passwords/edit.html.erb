<div class="container password-reset">
  <div class="row">
    <div class="col-md-6 offset-md-3 mt-md-5 mb-md-5">
      <div class="card text-center">
        <div class="card-header">
          <h2><i class="fas fa-user-plus", color="#4BAAF3"></i>&nbsp;&nbsp;パスワードの変更</h2>
        </div>
        <div class="card-body">
          <h5 class="card-title"><i class="fas fa-pencil-alt", color="#4BAAF3"></i>&nbsp;&nbsp;新パスワード作成</h5>
          <p class="card-text mt-4">6文字以上16文字以下の半角英数字で構成してください</p>
          <div class="p-3">
            <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>
              <%= f.hidden_field :reset_password_token %>

              <div class="form-group mt-0">
                <%= f.label :password, "パスワード", for: "formGroupInput", class: "bmd-label-floating" %><br />
                <%= f.password_field :password, autofocus: true, autocomplete: "new-password", class: 'form-control', id: "formGroupInput" %>
                <span class="bmd-help">6文字以上〜16文字以内、半角英数字記号で入力ください。</span>
              </div>

              <div class="form-group mt-0">
                <%= f.label :password_confirmation, "パスワード（確認）", for: "formGroupInput", class: "bmd-label-floating" %><br />
                <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control', id: "formGroupInput" %>
                <span class="bmd-help">6文字以上〜16文字以内、半角英数字記号で入力ください。</span>
              </div>
              
              <div class="actions mt-5">
                <%= f.submit 'パスワードの変更', class: 'btn btn-raised btn-primary mx-auto d-block w-75' %>
              </div>
            <% end %>
            <%= render "devise/shared/links" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>